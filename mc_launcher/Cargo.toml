# Cargo.toml
[package]
name = "mc_launcher"
version = "0.1.0"
edition = "2021"

[dependencies]
regex = "1.5"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
os_info = "3.0.0"
anyhow = "1.0"
actix-web = "4.0"
actix-cors = "0.6"  # 跨域支持